# Shared non-format defaults for week materials.
# IMPORTANT: do not set `format:` here — notebooks are HTML,
# slides declare revealjs themselves.
execute:
  freeze: auto
  warning: false
  message: false
