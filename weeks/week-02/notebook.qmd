---
title: "Woche 2 — Notebook · Erstes Modell"
subtitle: "Eine Population → Räuber–Beute"
format:
  html:
    toc: true
    css: ../../styles.scss
execute:
  echo: true
  warning: false
  message: false
---

::: {.callout-note}
**Sitzung:** nach ~45 min Vorlesung → ~45 min Notebook.  
Blöcke A/B üben die Pipeline aus den Folien: Daten → ein Schritt → Vektor → Schleife → Plot.
:::

## Lernziele (Notebook)

1. Eine Population: Differenzenschritt → Vektor → Schleife → Plot  
2. Qualitativer Vergleich mit `paramecium_alone.csv`  
3. Räuber–Beute: zwei Variablen, dieselbe Pipeline  
4. Einen Parameter ändern und biologisch kommentieren  

## Block A · Eine Population (~20 min)

### A1 Daten ansehen

```{r}
# alone <- read.csv("data/paramecium_alone.csv")
# plot(...)
```

**Frage:** Wachstum? Plateau?

### A2 Ein Schritt

```{r}
# next_N <- function(N, r, K) { ... }
# next_N(20, r = 0.5, K = 200)
```

### A3 Vektor + Schleife + Plot

```{r}
# N <- numeric(T + 1); N[1] <- ...
# for (t in 1:T) N[t + 1] <- next_N(N[t], r, K)
# plot(0:T, N, type = "l", ...)
```

### A4 Vergleich

Daten und Simulation nebeneinander. Qualitativ kommentieren (keine Perfektion nötig).

## Block B · Räuber und Beute (~20 min)

### B1 Daten

```{r}
# both <- read.csv("data/paramecium_with_didinium.csv")
# Immigration-Tage markieren (Spalte immigration)
```

### B2 `next_state` + Schleife + Plot

Wie in den Folien: zwei Vektoren `N`, `P`.

### B3 Parameter-Experiment

Nur **einen** Parameter ändern (z. B. `a`). Was passiert mit Beute / Räuber?

## Block C · Kurzreflexion (~5 min)

- Wichtigstes Coding-Muster heute:  
- Wichtigste biologische Idee:  

## Hinweise für Autor:innen

- Sprache: Deutsch, Erstsemester; R-Basics in den Folien schon skizziert (Funktion I/O, Vektor, for, plot/lines)
- Notebook soll **dieselben Namen** nutzen: `next_N`, `next_state`, `N`, `P`, `r`, `K`, …
- Aufgaben: Inputs variieren, einen Parameter drehen, kurz interpretieren
- Daten: `weeks/week-02/data/` + `README.md`
