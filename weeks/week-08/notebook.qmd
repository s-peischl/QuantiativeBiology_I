---
title: "Week 8 — Notebook · Toward establishment"
subtitle: "Many attempts; rare beneficial lineages"
format:
  html:
    toc: true
    css: ../../styles.scss
execute:
  echo: true
  warning: false
  message: false
---

## Learning goals

1. Contrast deterministic \(p_t\) thinking with single-lineage fate
2. Run many replicate introductions
3. Estimate an establishment frequency

## Design note for collaborators

Provide one simple stochastic lineage update (branching-process or WF with one mutant). Keep the API stable for Weeks 9–10.

## Starter scaffold

```{r}
# Pseudocode / fill-in: replace with the course's chosen update
run_one_lineage <- function(s, max_gen = 200, establish_threshold = 50) {
  n <- 1
  for (g in 1:max_gen) {
    # TODO: stochastic update using s
    # if n hits 0 -> lost; if n >= threshold -> established
  }
  FALSE
}

n_reps <- 500
s <- 0.05
established <- replicate(n_reps, run_one_lineage(s))
mean(established)
```

**Q1.** Write your operational definition of “established”.

**Q2.** Predict how `mean(established)` changes if you increase `s`.
