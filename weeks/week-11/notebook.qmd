---
title: "Week 11 — Notebook · Load complex microbiome data"
subtitle: "Abundance tables + metadata joins"
format:
  html:
    toc: true
    css: ../../styles.scss
execute:
  echo: true
  warning: false
  message: false
---

## Learning goals

1. Load abundance + metadata files
2. Diagnose ID mismatches
3. Compute library sizes and a short data passport

## Tasks

```{r}
# abund <- read.csv("data/abundance.csv", row.names = 1)
# meta  <- read.csv("data/metadata.csv")
# TODO: check setdiff(rownames(abund), meta$sample_id)
# TODO: library sizes <- rowSums(abund)
```

**Q1.** Wide or long? How can you tell?

**Q2.** List mismatches between abundance rows and metadata.

**Q3.** Summarize library sizes (min, median, max). Any alarming samples?
