# Week 12 microbiome tables

Placeholder. A teaching extract (abundance + metadata, maybe taxonomy)
will live here. Until then the notebook uses commented `read.csv` paths.

Expected files:

| File | Role |
|---|---|
| `abundance.csv` | samples × taxa (counts) |
| `metadata.csv` | one row per sample; column `sample_id` |
| `taxonomy.csv` | optional; taxon ID → phylum / genus |

Axis labels in plots: `Probe`, `Taxon`, `Abundanz`, `Bibliotheksgroesse`
(no umlauts).
