---
title: "Woche 12 — Notebook"
subtitle: "Tabelle und Schlüssel"
author: "Stephan Peischl, Loraine Hablützel und Emma Ochsner"
format:
  html:
    toc: true
    toc-title: "Inhalt"
    toc-depth: 2
    code-fold: false
    embed-resources: true
    css: ../../styles.css
execute:
  echo: true
  warning: false
  message: false
  freeze: false
---

::: {.callout-warning}
## Entwurf

Daten-CSV folgen. Bis dahin: Schema und die Prüfungen, die immer gelten.
:::

# Ziel

In ungefähr **45 Minuten**: einlesen, IDs prüfen, Bibliotheksgrösse.
Keine schöne Abbildung — eine ehrliche.

```{r}
# Projektordner: data/ liegt neben dieser Datei.
# abund <- read.csv("data/abundance.csv", row.names = 1)
# meta  <- read.csv("data/metadata.csv")
```

::: {.callout-tip}
## Aufgabe A — Einlesen

1. Welche Spalte ist der **Schlüssel** (Proben-ID)?
2. Ist die Abundanztabelle breit oder lang?
3. Warum öffnen Sie das als **Projekt**, nicht als einzelne Datei
   irgendwo auf dem Desktop?
:::

```{r}
# setdiff(rownames(abund), meta$sample_id)
# setdiff(meta$sample_id, rownames(abund))
```

::: {.callout-tip}
## Aufgabe B — Join

1. Listen Sie IDs, die nur in einer der beiden Tabellen vorkommen.
2. Was würde `merge` tun, wenn Sie das ignorieren?
3. Eine Probe ohne Metadaten: plotten Sie sie — oder legen Sie sie
   beiseite und schreiben das auf?
:::

```{r}
# lib <- rowSums(abund)
# summary(lib)
# hist(lib, xlab = "Bibliotheksgroesse", ylab = "Anzahl",
#      main = "QC")
```

::: {.callout-tip}
## Aufgabe C — Bibliotheksgrösse

1. Minimum, Median, Maximum.
2. Gibt es Ausreisser, die Sie vor Woche 13 klären müssen?
3. Warum ist *diese* Abbildung ein Nachweis — und ein Excel-Screenshot
   keiner?
:::
